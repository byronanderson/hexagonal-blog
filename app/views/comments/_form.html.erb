<%= form_for [@blog_post, @blog_post.comments.build] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.label :body, "Comment" %>
  <%= f.text_area :body %>

  <%= f.submit "Submit" %>
<% end %>
